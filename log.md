# Intern-Apply 经验库（结构化经验卡 + 情境索引）

> **设计原则（复用优先）**：经验的价值不在于"记下来"，而在于"下一轮能被可靠地用上"。每条经验都是一张**结构化卡片**，关键字段是 `触发信号`(trigger)——agent 据此判断"当前情境是否适用这条"。复用流程是「按阶段查索引 → 读触发信号 → 判断是否触发 → 触发则照对策执行」，不是"读全文碰运气"。

> **本库定位**：只保留**跨环境普遍、有复用价值、够格当 sample** 的卡。已升级成 SKILL.md 正文规则的，从本库删除（正文已是唯一来源，不重复存）；特定环境一次性数据、对策太笼统的，也不留。本库因此保持精炼——卡少而精，复用质量才高，同时这些卡本身就是"一张好经验卡长什么样"的示范。

> **关联**：流程定义在 `SKILL.md`；经验库的使用/收敛/升级规则见 SKILL.md「经验库机制」章节。

## 经验卡字段说明

```
EXP-XXX · [阶段]
- 触发信号: 什么情境/关键词下适用这条（复用的检索依据，最重要）
- 现象: 发生了什么问题
- 对策: 怎么解决（具体到可执行的动作）
- 验证: 已验证 / 推测未验证（已验证=实测跑通过；未验证=逻辑推断）
- 状态: active(生效) / superseded(被取代)
```

## 情境索引（按阶段 × 触发信号）— agent 进入每阶段先查这里

| 阶段 | 触发信号关键词 | 适用经验卡 |
|---|---|---|
| 阶段1 采集 | 404 / 页面不见了 / token / 登录失效 / 提示登录 | EXP-001 |
| 阶段1 采集 | 限流 / 返回慢 / 空响应 / 风控 / 封号 / 单平台不可用 | EXP-002 |
| 阶段1 邮箱提取 | 中文连邮箱 / 前缀 / OCR邮箱 / 视觉识别 | EXP-003 |

---

## 经验卡（按编号）

### EXP-001 · [阶段1采集] 访问详情页受阻
- 触发信号: 直接访问详情页返回 404 / 页面不见了 / 链接里带 token/xsec_token/session 参数 / 临时浏览器 profile / 拿不到登录页内容 / 提示登录
- 现象: 详情页有访问凭证（token/session）裸 URL 直接访问拿不到内容；或登录态失效，临时 profile 无 cookie
- 对策: 从列表页/搜索页的链接里提取凭证参数拼到详情 URL 再访问，或走浏览器登录态点击进入；用用户已登录的浏览器 profile（临时 profile 无 cookie）
- 验证: 已验证（多轮实战）
- 状态: active

### EXP-002 · [阶段1采集] 平台限制采集
- 触发信号: 批量请求失败增多 / 返回慢 / 空响应 / 被限流 / 某平台风控 / 封号 / 地区限制 / 一直失败
- 现象: 平台限制批量采集——限流（请求过多）或风控/封号（单平台不可用）
- 对策: 降速分批（每请求间隔 2s+，失败跳过不重试，100-150/批）；仍不可用则换来源（官网 API / 其他平台 / CLI），不吊死一棵树
- 验证: 已验证
- 状态: active

### EXP-003 · [阶段1邮箱提取] 邮箱提取质量
- 触发信号: 中文引导词紧贴邮箱（如"简历请投递到xxx@yyy.com"）/ 邮箱前有中文 / 从图片/海报 OCR 提取的邮箱 / 邮箱来自视觉识别
- 现象: 提取的邮箱不可靠——中文引导词和邮箱连成整体被当成收件人；或 OCR 识别有字符错误（l/1、O/0、漏字符）
- 对策: 提取后剥前缀（剥离中文引导词）再校验；投递前小样本试发验证，不要对未验证的邮箱批量发送
- 验证: 已验证
- 状态: active

## 收敛规则（去重 / 合并 / 升级 / 老化）

1. **去重**：新经验与现有卡触发信号高度重合 → 不新增，更新现有卡的对策（或追加为"环境特化版"，标注环境差异）。
2. **合并**：多条卡描述同一问题的不同表现 → 合并触发信号（用"或"连接），对策取最全的。
3. **升级（经验→规则）**：同一卡在运行中重复触发 ≥3 次（说明靠经验提醒不够，必须变成强制规则），或对策已稳定可靠 → 把对策写进 SKILL.md 正文，**然后从本库删除这张卡**（正文已是唯一来源，不重复存）。
4. **老化**：状态变 superseded（被新对策取代）的卡，从索引和经验卡区一起删除（历史在 git 可追溯）。

## 运行记录（新经验追加区）

（agent 每轮在此追加：新增的经验卡编号 + 简述；收敛动作记在此处并标注日期）
