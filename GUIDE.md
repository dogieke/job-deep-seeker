# 首次引导（阅后即焚）

欢迎使用 job-deep-seeker。运行前：① 读 SKILL.md 的核心目标；② 花 2 分钟提供你的信息（姓名/学校/毕业时间/可用性/城市/能力素材线索），agent 会帮你探测环境、找岗位、生成定制简历并投递。

> **资源清单**：本包自带 `templates/`——`resume-modern/`（默认简历模板+风格）、`sample-resume/`（写法范式）、`toolbox/`（工具样本库）。需要时按需读取。

与 agent 过完上面两点后，删除本文件（含下方的参考实现），正式开工。

> **冷启动机制（重要）**：本文档（含参考实现）是**冷启动引导**——只在首次运行时读。当 agent **完整跑通一轮流程**（从信息探测到投递）之后，**删除本文件的参考实现示范**，之后 agent 完全按自己的环境、自己的情况自由发挥，不再参考任何写死的实现。

## 参考实现（仅逻辑参考，勿照抄）

> 以下来自特定环境（macOS + 中国招聘平台）。**看思路，不抄命令**——具体实现按环境自行适配。

### 找岗位
- **社交平台**（小红书等）：已登录浏览器 profile 打开搜索页 → 收集链接 → 详情页需要访问凭证（token），裸 URL 会 404。**反爬：截图 + 视觉模型转写，不爬取**
- **大厂官网 API**：有现成 CLI（如 jobhunt-cli 类，npm 安装）直连招聘 API，无需登录

### 提取邮箱
- 正则提取邮箱；**脏前缀坑**：中文引导词会和邮箱连成整体（"简历请投递到xxx@yyy.com"），必须剥前缀再校验

### 发送邮件
- 本机邮件客户端（macOS Apple Mail）/ SMTP（跨平台，需授权码）/ 网页邮箱自动化（零配置，浏览器操作）
- 思路：先本机 → 找开源工具 → 浏览器兜底
- 限流：免费邮箱日限 ~100-500，建议 50/批，间隔几秒

### 简历 PDF
- 用随包模板编辑 HTML → HTML 转 PDF（目标单页 A4）

### 作品集（可选）
- GitHub Pages 部署，验证 HTTP 200；链接放邮件签名
