---
name: job-deep-seeker
description: 全自动求职投递流水线（实习/全职/校招均适用）。目标驱动的通用工作流：自动寻找岗位信息（多来源）→ 自动整理用户素材 → 与用户协作生成简历模块 → 组合成适配简历 → 自动邮箱投递 → 记录跟进。所有工具/配置由 agent 通过自检+网络搜索+测试发现，本 skill 不绑定任何具体工具，原实现仅作参考例子。
metadata:
  version: "3.0.0"
  last_updated: "2026-08-05"
  license: MIT
---

# Intern Apply — 目标驱动的求职投递工作流（实习/全职/校招均适用）

> **首次运行先做引导**：若同目录存在 `GUIDE.md`，先读它并与用户过一遍（收集基本信息 + 确认理解），完成后**删除 GUIDE.md**（阅后即焚，避免每次运行都背着引导负担）。

> **设计原则（核心）：不写死环境**。现在的模型能力已经很强，把环境/工具写死反而会限制 agent。每一步只规定【目标】，实现方式由 agent 探测 + 搜索 + 测试发现，引导用户配合配置。

> **资源按需读取（重要：勿全量加载）**：`templates/` 里的资源（模板/风格/样本/工具箱）**只在对应阶段需要时读取**——生成简历时读 resume-modern/，缺工具时读 toolbox/，组织素材时读 sample-resume/。不要一次性把所有资源读进上下文（省 token、聚焦）。

## 核心目标（不可省略，全部必须达成）

```
① 找到足够的岗位信息（含投递方式：邮箱/表单/内推）
② 整理用户的真实素材 → 与用户协作加工成「简历模块」
③ 每岗位：模块组合 → 生成适配该岗位的简历
④ 自动投递（邮箱直投为主）
⑤ 记录 + 跟进回复 + 沉淀教训
```

**任何一步没有可用工具，都不是停下来的理由**——去探测、搜索、测试，找到替代方案。

## 阶段 0：初始化（发现式，勿跳过）

**目的**：摸清环境能力 + 收集用户信息。全部通过自检/搜索/询问完成，不假设任何预置。

### 0a. 收集用户 resume 信息（关键：主动引导，不只是问）

**目标**：收集用户的全部简历材料。用户很可能不知道自己有哪些东西可以用——**你要考虑全，在发问中尽量引导用户补充更多材料**。

**收集顺序**：
1. **已有成果**：用户已经写好的简历（PDF/Word/网页）→ 让用户直接提供文件或路径
2. **作品集/作品**：作品集链接、文章、论文、代码仓库、账号数据（播放记录/粉丝量/作品链接）→ 问用户要链接/文件
3. **环境搜寻**：在用户环境里搜索可用的材料（作品目录、数据导出、文档）——先搜再问，搜不到的问用户
4. **补全缺失**：对照下表逐项确认，缺什么问什么

**必收集字段**：
| 字段 | 说明 |
|---|---|
| 姓名/年龄/学校/专业/毕业时间 | 简历与邮件签名 |
| 可用性（可到岗时间/每周几天/工作时长） | 邮件主题与安排段 |
| 投递邮箱 + 电话 | 发件账户 + 联系方式 |
| base 城市 | 可到岗地点 |
| 能力与素材线索 | "你做过什么项目/写过什么/有什么作品和数据？" |

**引导技巧（关键）**：用户常低估自己的材料。主动追问：
- "除了简历，你有没有写过文章/做过项目/运营过账号/做过数据分析/发表过东西？"
- "有没有 Git 仓库、作品集链接、写作作品、播客/视频？"
- "有没有得过奖/被老师认可/有数据支撑的成果？"
- 每一项素材都问清楚：**产出什么 + 量化结果 + 可验证来源**

### 0b. 探测环境能力（关键：不写死，引导配置）

**第一步：检查 agent 自身能力**（提示词层面自查）：
```
① 我有联网能力吗？（web_search/fetch 可用？）
② 我有浏览器操作能力吗？（能打开网页/截图/点击？）
③ 我能读写文件、执行命令吗？
```
**如果没有，引导用户配置**——不要自己硬找，先问用户环境里有什么。

**第二步：探测环境工具**（探测哪些能力，具体命令 agent 自己会）：
```
① 基础：python3 可用？
② 浏览器自动化：能打开网页/搜索/滚动/截图/点击吗？
③ 邮件发送：本机有邮件客户端？有 SMTP 授权码？网页邮箱可登录？
④ 文档转 PDF：有 HTML→PDF 工具吗？
⑤ 网络：能访问目标平台？是否需要代理？
⑥ 视觉模型：有多模态模型 API？（用于反爬页面截图识别，见阶段①）
```

**自检不通过 → 引导用户 + 以样本为参考**（不是写死方案）：
```
① 先问用户：你环境里有什么可用的？
② 查 skill 自带「工具样本库」（templates/toolbox/README.md）——里面是 GitHub 上验证过的高 star 工具链接 + 案例示范
③ 以样本为「找什么」的参考，用 web_search 搜当前环境可用的同类工具
   （链接失效也没关系：样本告诉 agent 找哪类工具，同类项目即可）
④ 测试：小规模试跑验证可用性
```

**测试通过后**：把「环境能力清单」记入 `{工作目录}/env.json`，本轮复用。

## 阶段 ① 找岗位（多来源，自动发现）

**目标**：拿到 ≥N 个有效岗位（含投递方式）。**N 由用户设置**——启动时问用户一次「这轮想投多少个岗位？」（用户说 20 就是 20，说 50 就是 50；不问就默认 50）。

**信息侦探（多来源并行）**：
- **渠道原则（明确可执行）**：
  1. 先把已知渠道都试一遍（下面的渠道清单 + 自行扩展的）——这是底线，不是可选项
  2. 启动时问用户一次：有没有你想投的特定渠道/平台？（用户可能知道，也可能说没有）
  3. 每个渠道实际尝试；不通 → 换替代继续，直到岗位够 N 个
  4. 渠道状态（哪个通了/没通/为什么）记入岗位库
1. **公开信息采集**：本地已装的求职 CLI（如 JobHunt-CLI 类）或搜索发现的开源求职工具——结构化岗位数据，无需登录
   - 先用 `npm`/`pip` 探测已装 CLI；没有 → 查「工具样本库」找同类 → 搜 GitHub 高 star 项目
   - **务必真实、star 数高**：用 web_search 确认项目真实性（star 数、维护活跃度）
2. **浏览器操作采集**（社交平台）：引导用户登录自己的社交账号（如小红书），agent 用浏览器操作搜索岗位关键词
   - **反爬纪律（必须遵守，并向用户交代清楚）**：不要用爬虫/直接请求手段采集——会被网站屏蔽。正确做法：
     a. 浏览器打开页面 → **截图** → 调**视觉多模态模型**把截图转写成结构化信息（标题/正文/邮箱/投递方式）
     b. 需要视觉模型：如果 agent 自己没有，**要求用户配合提供一个视觉模型**（多模态 API）
   - 经验参考：搜索页链接可能带访问凭证（token），详情页裸 URL 会 404——先截图识别，不要硬爬
3. **官网招聘页**：目标公司 careers 页（浏览器打开/抓取）

**岗位信息沉淀（重要）**：收集到的岗位信息（含投递方式）不仅是当轮用，还要**沉淀到素材库**（与用户个人素材隔离，单独目录），作为以后：
- 投递决策参考（哪类岗位多、投递方式分布）
- 生成简历/组合简历的参考信息（JD 要求 → 简历侧重）

**每个来源的自检点**：
- 能拿到什么字段？有没有【投递方式】（邮箱优先）？
- 登录态/风控？失败率高 → 降频 or 换来源
- 结构化程度？能自动解析还是要浏览器+视觉识别？

**去重**：跨来源按「公司+岗位名」去重，避免重复投递。

**岗位确认（冷启动首轮做一次）**：
- 第一轮启动时，把筛选后的候选岗位（公司/岗位/方向/投递方式）给用户看一次，确认投递范围与方向
- 确认后沉淀为用户偏好，后续轮次按此推进，不再列列表（用户主动要看随时给）

## 阶段 ② 素材整理（agent 自主 + 用户协作）

**目标**：把用户的真实产出整理成可复用的「简历模块」（素材卡）。

**素材发现**（自动扫描，问用户补充）：
- 用户提到/展示的项目、作品、论文、代码、账号数据
- 文件系统里可发现的（如作品目录、数据导出）
- 平台数据（播放记录、粉丝量、作品链接等，如用户愿意提供）
- 阶段 ① 沉淀的岗位信息（JD 要求 → 简历侧重参考）

**整理动作**（每素材一张卡，存入素材库）：
```
素材卡 = { 证据(可查证) + 量化指标(如有) + 来源 + 可支撑能力 + 岗位适用性 }
```

**默认优化方向（样本驱动）**：skill 自带一份「sample 简历」（脱敏的真实简历，见 `templates/sample-resume/`）——它展示了高质量简历的写法范式：
```
每个经历 = 在什么岗位/角色工作 → 工作范围是什么 → 有什么产出（量化） → 如何持续迭代优化
```
- 这个范式是**默认优化方向**：素材卡/简历都按这个思路组织
- 用户可以选：采用这个默认方向，或提出自己的偏好（布局/重点/风格）

**验证纪律**：
- 素材必须真实（本人产出/可验证）——这是底线，不能编造
- 不确定的素材 → 问用户确认，不擅自采用
- 其余表述尺度（如何突出、多大胆）由 agent 与用户商定，尊重用户偏好

**与用户协作**：把整理好的素材卡/能力卡给用户过目，确认哪些可用、哪些需要补充。**产出「简历模块库」**（每个模块 = 一条可在简历中展开的能力表述 + 支撑素材）。

## 阶段 ③ 组合简历（模板 → 模块 → 适配岗位）

**模板策略（必读，先问用户）**：
- 用「默认简历模板」还是「自定义偏好」？**先问，不要直接默认**
- **默认模板**：随 skill 分发的 `templates/resume-modern/resume.html`（A4 单页、苹果风、占位符版）——只填占位符，不改变结构；模板结构规范见同目录 `SKILL.md`
- **自定义偏好**：用户选视觉风格（`templates/resume-modern/styles/` 有 6 种：苹果/极简/小红书/复古/商务/创意）→ 套用对应 tokens.css 调整颜色字体，布局/栏目也可按用户偏好改，见 `templates/resume-modern/README.md`
- 模板版权与来源：`templates/resume-modern/README.md` 文末（Open Design Apache-2.0）

**流程**：
1. 确认模板（默认 or 自定义）→ 用模板建「底板简历」（1-2 份高质量底版，作为质量基准）
2. 每岗位：从模块库选相关模块 → 与底板混合 refine（补充/替换/定制段落）→ 成品简历
3. 导出 PDF（或岗位要求的格式）

**纪律**：
- **从模块拼装 ≠ refine**：底版是质量基准，模块只做补充/定制，不从零拼装
- 金融/专业素材按岗位定制（音乐/内容岗不写金融，金融/产品岗才加）
- 内容永远按用户真实素材组织（不虚构、不拼装）
- 写法参照 sample 简历范式：岗位→范围→量化产出→持续迭代

**PDF 生成（用规定模板，不让 agent 自由生成）**：
- 直接用随包模板 `templates/resume-modern/resume.html`（A4 单页、苹果风）——agent 只编辑 HTML 内容（占位符/模块），不改样式结构
- 原因：让 agent 从零生成 PDF 很容易出代码/前端问题，即便代码对美观也不足——模板是我们整合的资源，直接用最好
- 转换：HTML → PDF，目标单页 A4（浏览器打印或 HTML→PDF 工具，agent 自选）

## 阶段 ④ 投递（邮箱直投为主，方式按环境发现）

**目标**：把简历 + 定制邮件发到每个岗位的投递邮箱。

**邮件生成（核心规则，必须遵守）**：
```
Subject: 应聘{岗位} - {姓名} - {可用性一句话}
正文:
【申请方向】我申请【{方向}】，因为：
  1. {匹配理由1，具体素材}
  2. {匹配理由2，具体素材}
  3. {匹配理由3，具体素材}
【一句话介绍】{学校}（{毕业时间}毕业）
【安排】{可用性}，base {城市}（可远程）
【作品集】{如有}
简历详见附件...
```

**方向判定**：从岗位标题/描述关键词自动判断（产品/增长/运营/数据/AI...），匹配理由从模块库抽。

**发送方式发现（不写死，思路：先本机 → 找开源 → 浏览器兑底）**：
```
① 先探测用户环境有什么（本机邮件客户端 / SMTP / 网页邮箱 / 邮件 API）
② 选一个能用且最简单的，向用户说明【为什么这么做】（让用户有知情权：用最简单可靠的方式投递，不折腾环境）
③ 没有 → 搜 GitHub 同类工具 → 再不行 → 浏览器操作网页邮箱兑底
```

**投递纪律**：
- **单封测试先行**（首封验证到达再批量）
- 批量间隔几秒，防限流
- 发件账户 + 签名统一

## 阶段 ⑤ 记录 + 跟进 + 沉淀（双轨沉淀）

**沉淀有两方面，都要做**：

**① 市场/投递信息沉淀（记录投递情况）**：
- 每封投递记录（时间/收件人/岗位/附件版本/方向）到 `{工作目录}/applications.jsonl`
- 岗位信息沉淀到素材库（与用户个人素材隔离，见阶段①）
- 用途：
  - 用户随时知道投递情况（投了多少/投给谁/哪些方向）
  - 投递决策参考（哪类岗位回复率高）
  - 优化投递文案（哪些话术有效）
- **归档**：邮件文案按轮次归档

**② 执行沉淀（agent 错题集）**：
- 退信/回复/执行问题 → 写入错题本 `log.md`（带日期+问题+修复+适用阶段）
- **agent 下轮先读错题本**，直接用它 refine 自己的操作（避免重犯）
- 这正是 agent 自我迭代的机制：每轮执行都变好一点

**跟进**：定期检查收件箱，收到回复 → 整理对方背景 → 起草回复 → **用户确认后才发出**（涉及承诺的绝不擅自回复）

## Self-Refine（自修补机制，贯穿全程）

**原则**：遇到问题不卡死、不跳过——诊断 → 修复 → 继续。**修复的经验必须记入错题本**（见「错题本机制」），成为下轮已知坑。

### 自检点（每步完成后）

| 阶段 | 自检 | 异常处理 |
|---|---|---|
| 初始化 | 工具探测完整？ | 缺的进搜索发现流程 |
| 找岗位 | 岗位够 N 个？投递方式有吗？ | 不够 → 换来源/扩关键词 |
| 素材整理 | 每素材可查证？能力 ≥2 素材？ | 不足 → 降级表述/问用户 |
| 组合简历 | 模块与岗位匹配？质量达标？ | 换模块/回底版 refine |
| 投递 | 首封测试过？有退信？ | 修邮箱/换发送方案 |
| 记录 | 日志一致？ | 合并去重 |

### 工具替代发现（核心机制）

**当某阶段没有可用工具时**：
```
① 先问用户：你环境里有什么可用的？
② 查「工具样本库」templates/toolbox/README.md——知道该找哪类工具
③ 以样本为参考，web_search 搜同类高 star 开源项目（链接失效没关系，找同类即可）
④ 评估：能达成【该阶段目标】? 配置成本? 生态?
⑤ 测试：小规模验证可用性
⑥ 记录：适配方案写回 env.json + log.md（含工具名+用法，供下轮复用）
```

**判断标准**：只要求达成阶段目标，不追求与原实现一致。**不写死**：任何工具都只是当前环境的临时选择，换环境重新探测。

### 修补记录规范

每修补写 log.md。**不打断用户**，除非：
- 需要新权限/凭据（如 SMTP 授权码）
- 涉及金钱/承诺/个人信息对外发布
- 连续 3 次同类修补失败（需重新设计）

## 错题本机制（经验积累，自动增长）

**错题本文件**：`本skill目录/log.md`（随 skill 一起分发，自带种子案例）

**机制**：
1. **种子案例**：已内置开发者实战经验（泛化清洗：去个人信息、去特定环境），覆盖采集/邮箱/邮件/简历/回复 5 类常见坑
2. **运行追加**：每轮结束，agent 把「新问题 + 修复」追加到 log.md 运行记录区（`[日期] 问题 | 修复 | 适用阶段`）
3. **沉淀引用**：下轮运行先读 log.md 已知坑，避免重复踩
4. **贡献回 skill**：若某坑适用面很广，回写 SKILL.md 正文（但正文只放方法论，不放环境细节）

**效果**：skill 是骨架，错题本是血肉——每个用户/环境的实战经验都沉淀下来，随使用增长，且不污染核心流程。

---

## 已知坑（完整见错题本 log.md）

> 完整已知坑见同目录 `log.md`（种子案例 + 运行记录）。正文只保留需要用户确认的规则：

| 坑 | 对策 |
|---|---|
| 招生/科研岗误投 | 用户要求时过滤：教授招生/Lab/纯研究/校招不投（需用户确认规则） |
