---
name: job-deep-seeker
description: 全自动求职投递流水线（实习/全职/校招均适用）。目标驱动的通用工作流：自动寻找岗位信息（多来源）→ 自动整理用户素材 → 与用户协作生成简历模块 → 组合成适配简历 → 自动邮箱投递 → 记录跟进。所有工具/配置由 agent 通过自检+网络搜索+测试发现，本 skill 不绑定任何具体工具，原实现仅作参考例子。
metadata:
  version: "3.2.0"
  last_updated: "2026-08-09"
  license: MIT
---

# Intern Apply — 目标驱动的求职投递工作流（实习/全职/校招均适用）

> **渐进式披露（本文档使用方式）**：本文件是**流程骨架**——每次执行必读，建立全局心智。细分内容按需读取分文档，下面是触发式索引（什么时候读什么）：
>
> | 分文档 | 读取触发时机 |
> |---|---|
> | `GUIDE.md` | **首次运行**（读一次，跑通一轮后删除） |
> | `log.md` | **进入每个阶段前**（查顶部情境索引，复用适用经验卡）；**每轮结束**（写新经验卡） |
> | `vision-models.md` | **首次需要视觉转写时**（选哪个多模态模型、选型方法、实测证据） |
> | `templates/toolbox/` | **某阶段缺可用工具时**（找替代工具的同类样本） |
> | `templates/resume-modern/` | **生成简历时**（模板 + 风格 + 结构规范） |
> | `templates/sample-resume/` | **组织素材时**（写法范式：岗位→范围→量化产出） |
>
> 勿全量加载——只在对应触发时机读对应分文档（省 token、聚焦）。

## 核心目标（不可省略，全部必须达成）

```
1. 找到足够的岗位信息（含投递方式：邮箱/表单/内推）
2. 整理用户的真实素材 → 与用户协作加工成「简历模块」
3. 每岗位：模块组合 → 生成适配该岗位的简历
4. 自动投递（邮箱直投为主）
5. 记录 + 跟进回复 + 沉淀教训
```

**任何一步没有可用工具，都不是停下来的理由**——去探测、搜索、测试，找到替代方案。

## 阶段 0：初始化（发现式，勿跳过）

**目的**：摸清环境能力 + 收集用户信息。全部通过自检/搜索/询问完成，不假设任何预置。

**先定工作目录**（env.json/applications.jsonl/素材库都放这里）：agent 在用户桌面/文档下新建一个本轮投递工作区（如 `~/Desktop/投递工作区/`），或用户指定已有目录——所有产物集中存放，避免散落。

### 0a. 收集用户 resume 信息（关键：主动引导，不只是问）

**目标**：收集用户的全部简历材料。用户很可能不知道自己有哪些东西可以用——**你要考虑全，在发问中尽量引导用户补充更多材料**。

**收集顺序**：
1. **已有成果**：用户已经写好的简历（PDF/Word/网页）→ 让用户直接提供文件或路径
2. **作品集/作品**：作品集链接、文章、论文、代码仓库、账号数据（播放记录/粉丝量/作品链接）→ 问用户要链接/文件
3. **环境搜寻（agent 主动）**：让用户只提供检索线索（关键词/大致位置/主题词），**agent 自己在用户环境里找路径**（作品目录、数据导出、文档），不让用户自己翻文件——目的是探索出用户自己都可能不知道能用的材料；搜不到再问用户
4. **补全缺失**：对照下表逐项确认，缺什么问什么

**必收集字段**：
| 字段 | 说明 |
|---|---|
| 姓名/年龄/学校/专业/毕业时间 | 简历与邮件签名 |
| 可用性（可到岗时间/每周几天/工作时长） | 邮件主题与安排段 |
| 投递邮箱 + 电话 | 发件账户 + 联系方式 |
| base 城市 | 可到岗地点 |
| 能力与素材线索 | "你做过什么项目/写过什么/有什么作品和数据？" |

**引导技巧（关键）**：用户常低估自己的材料。主动追问：

- "除了简历，你有没有写过文章/做过项目/运营过账号/做过数据分析/发表过东西？"
- "有没有 Git 仓库、作品集链接、写作作品、播客/视频？"
- "有没有得过奖/被老师认可/有数据支撑的成果？"
- 每一项素材都问清楚：**产出什么 + 量化结果 + 可验证来源**

### 0b. 配置采集工具（关键：强制配齐，不许跳过——这是防偷懒的核心）

**核心原则（防偷懒）**：采集阶段的工具**不是"有就用没有就算"，而是必须主动配齐至少多个可用工具**。agent 天然倾向偷懒——只走最省事的渠道（如 web_search）、把"探测"当成"尝试"、凑够一点就停。为堵死这条偷懒路径，阶段0b 设硬门槛：**配不齐不许进采集阶段**。

**硬门槛（进阶段1 前必须全部达成，少一项都不许过）**：

| 工具类别 | 要求 | 验证方式（必须实测，不许只评估） |
|---|---|---| 
| **采集工具 1：求职 CLI** | **必须装好一个可用的结构化求职 CLI 并跑通**（直连招聘 API、无需登录、结构化字段最全） | 真的安装（npm/pip/二进制）→ 真的跑一次查询 → 拿到真实结构化结果；装不上或跑不通要换同类 CLI 直到跑通一个 |
| **采集工具 2：浏览器自动化** | **必须能用浏览器打开页面、截图、点击**（用于社交平台采集+详情页提取） | 真的打开一个网页并截图验证；不能则装 playwright/puppeteer/同类 |
| **采集工具 3：web_search** | 默认可用（搜索引擎检索） | 跑一次搜索确认有结果 |
| **放行门槛** | **采集工具数 ≥ 3 才放行进阶段1**（求职CLI + 浏览器自动化 + web_search 全部跑通） | 自检表「初始化」项不放行 | 

**配不齐怎么办（不许跳过，必须做到）**：
- 求职 CLI 装不上 → 以样本为参考找同类（1) 查 `templates/toolbox/README.md` 工具样本库找高 star 同类；2) web_search 搜「jobhunt CLI 替代」「开源求职工具」；3) 每个候选实测能否跑通）→ 换一个直到跑通。
- 浏览器自动化不可用 → 装 playwright/puppeteer 或本机浏览器驱动，实测打开+截图。
- **连续 3 个候选都跑不通**：诚实向用户报告"本环境确实无法配齐 3 个采集工具，已实测 X/Y/Z 均失败"，让用户决定是否降标准——但**不许未实测就说"不通"**。

**其他能力**（投递阶段需要，单独验证）：邮件发送（agent 自动探测，不能直接发则装 SMTP/邮件工具）、文档转 PDF（装同类工具）、多模态视觉模型 API（首次需要视觉转写时配，见阶段1.1）。

**配置同步纪律**：每装好一个工具向用户报进度（不要闷头配很久不说话）；不先问用户"环境里有什么"（用户多半不知道），agent 自己探自己配，只在需要凭据/权限时才求助。

**配齐后**：把「环境能力清单 + 每个工具的实测结果」记入 `{工作目录}/env.json`，本轮复用。

## 阶段 1 找岗位（采集池 → 择优，多来源主动并行）

> **关键重构：采集量与投递量解耦**。N（投递数量）只决定**最终投多少**，**不决定采集到多少就停**。哪怕用户只要 20 个，也要先把各渠道都打一遍形成候选池，再从池里择优 20 个。数量越少越要靠多渠道择优保质量——单一渠道凑够的 20 个，质量远不如四渠道择优的 20 个。

### 阶段1.1 采集子阶段（目标：充分覆盖，不是凑够 N）

**启动确认（问一次）**：1) 这轮想投多少个岗位？（=N，不问默认 50）2) 有没有指定的渠道/平台？（没有的话 agent 主动把下面渠道都打一遍）

**渠道强制 checklist（每个渠道必须有实际采集产出才算完成；不许只用最省事的渠道）**：

| 渠道 | 做法 | 本轮状态（必须实测后填，不许提前评估后跳过） |
|---|---|---|
| A. 结构化求职 CLI | 用阶段0b 配好的求职 CLI 跑查询，拿真实结构化结果 | F通（实采集 X 个）/ P部分 / X不通+实测证据 |
| B. 社交平台（小红书等） | 用户登录态浏览器 → 搜索 → **必须进详情页二次提取**（见下方纪律） | F / P / X+原因（未采集不算完成） |
| C. 目标公司官网 careers | 浏览器打开/抓取目标公司招聘页 | F / P / X+原因 |
| D. 公开网页/搜索引擎 | web_search 搜岗位+城市+邮箱；高校就业网/政府就业中心/行业社区 | F / P / X+原因 |

> **关键："评估"不是"尝试"。** 偷懒的常见手法是"看一下觉得不好用就跳过"（如这次测试 agent 把求职 CLI 停在"无预装"评估没真装）。每个渠道必须有**真实的采集动作和真实产出记录**才填 F/P/X；只评估未实测 = 未完成。

> **采集池硬门槛**：候选池总数 **≥ N×2** 才许进阶段1.2 择优（如 N=20 则采集池 ≥40）。达不到不许择优、不许让用户确认方向，回去继续补打渠道。这是防止"采集到一半就凑够停"的硬约束。

> 上表是候选渠道清单，不是优先级排序。各渠道在本环境的实际产出要采集后才知道——先全打一遍，再按实际产出收敛（见下方纪律）。

**渠道纪律（核心：多找 + 先发散后收敛，违反应在自检里被抓）**：

- **多找渠道**：启动多渠道并行（A/B/C/D），不预设渠道优先级、逐个实际尝试并填状态（没尝试 ≠ 不通），不主动扩展渠道 = 失败，禁止只用一个渠道凑够 N 就停。
- **多找目标岗**：每个渠道多筛不同岗位/方向，别在一两个岗位上钻牛角尖。
- **先发散后收敛**：启动尽量多打 → 跑出实际产出后收敛（高产出/高可信的留下重点化，低效/被限流的降级为存档参考），收敛依据本轮实际状态记录，非预设排序。**各渠道状态（哪个通/哪个不通/为什么）记入岗位库，向用户透明汇报**（防只走 1 个渠道隐瞒）。
- **发现可靠集中信息网络（高校就业网/政府招聘会等，官方背书+集中批量+邮箱可达）重点打深**——发现后动态重点化，非启动预设。

**社交平台采集纪律（反爬 + 流程完整）**：

- **反爬**：不用爬虫/直接请求（会被屏蔽）。正确做法：浏览器打开 → 截图 → **多模态视觉模型**转写结构化信息。
- **多模态模型选择（首次询问用户，不写死、不让 agent 自作主张）**：
  - 视觉转写需要多模态模型。**首次需要视觉转写时，向用户确认用哪个多模态模型**（给出候选由用户拍板），选定后沿用本轮，记入 `{工作目录}/env.json`。
  - **为什么必须问用户**：避免「低价模型指挥高价视觉模型」的荒唐局面——若主对话模型是低成本/非视觉模型，让它自己决定调用哪个视觉模型，等于让低价指挥高价，选型可能不合理；由用户拍板才可靠。
  - **用户不确定用什么时 → 发包给 subagent 确定**：把「确定当前环境最便宜、能稳定转写中文的多模态模型」作为独立任务交给一个 subagent（独立模型）执行——它负责探索环境已配 key 的 provider、查证当前价格、用小样本图实测，返回一个明确推荐。跨模型独立研究能避免单一模型的主观偏差。
  - **通用选型参考（不写死；运行时按当时环境/价格复核）**：低成本多模态视觉模型就够用，**不追贵的**——选一个环境里现成、便宜、实测能稳定转写中文的多模态即可。（需要选型时，读取同目录 `vision-models.md`：含可选模型清单、实测证据、价格、选型方法）。
  - **推荐比较方案（怎么选候选，可靠且不繁重）**：
    **禁止**：不写批量对比脚本、不跑多模型算准确率、不做性价比表（过度工程）。
    **怎么做**：候选（用户给的或 subagent 推荐的）**每个用一张真实中文小样本反爬图跑一次**，只看**关键字段（中文岗位名 + 邮箱）是否转写正确**——正确即可用；乱码/失败/返回空即淘汰换下一个。**都转写正确时，选价格最低的**。选定后沿用本轮。
    **价格从哪来**：优先读环境已配模型的价格（如 models.json 的 cost 字段：单价或订阅内 0），不确定真实市场价时由 subagent 查证（web_search 官方定价页）。
    （注：此处「OCR」指多模态 OCR 视觉模型，非 tesseract 传统 OCR，两者不同）。
- **视觉诚实性（硬规则，必须遵守）**：如果当前模型**没有真实的多模态能力（读不了图），或读到了但结果存疑**，**必须诚实声明**，绝不编造识别结果。读不到就标「未获取/无法确认」，**严禁凭常识编造**职位/公司/薪资/JD/邮箱。若确实无任何可用多模态，再降级 OCR，仍不行则放弃截图改纯文本提取（进详情页正则提取邮箱/正文解析）。**编造视觉结果是最严重的诚信违规**——会污染岗位库、误导投递、毁掉用户信任。
- **详情页二次提取（社交平台必做）**：列表页/搜索页通常**没有投递方式**（邮箱/微信/电话往往在帖子图片或正文里）。流程必须是：搜索页**只筛岗位和方向** → **逐条点进详情页** → 从详情页正文+评论提取邮箱/投递方式 → 才入库。列表页截图转写完直接入库是错的（测试已暴露：列表页几乎拿不到联系方式）。
- 经验：详情页链接可能带访问凭证（token），裸 URL 会 404——从列表页链接里提取凭证再访问，或走浏览器登录态点击。

### 阶段1.2 择优子阶段（目标：从候选池输出 N 个最优）

候选池（多渠道采集的并集，通常 >N）→ 按以下维度排序，取前 N：
1. **简历方向匹配度**（与用户素材库/简历方向的相关性，最重要）
2. **投递方式可达性**（邮箱直投 > 表单/平台投递 > 仅私信；邮箱直投优先投）
3. **渠道可信度**（官网/高校就业网/政府审核 > 来源不明的搜索结果）
4. **风险排除**（见下方风险清单，高风险直接剔除）

去重：跨来源按「公司+岗位名」去重，避免重复投递。

### 阶段1.3 投递前岗位验证（按风险分级，不必全做）

不是每个岗位都做全套验证（成本太高），按风险分级：
- **必做（低成本，所有岗位）**：邮箱格式 + 域名 MX 记录（确认是真实邮件服务）；风险特征筛查（见下）
- **可疑才做（高风险信号触发）**：公司工商信息（天眼查/企查查/国家信用公示 gsxt.gov.cn，核对成立时间/注册资本/参保人数）；地址电话与工商登记一致性
- **触发的风险信号**：高薪低门槛、先交钱/押金、扣押证件、培训贷、异地入职、限性别/地域、留电话但无公司名
- **方向过滤（需用户确认）**：教授招生/Lab/纯研究/校招不投（用户要求时过滤，防误投科研岗）

### 阶段1.4 岗位沉淀 + 确认

**沉淀**：采集到的岗位（含投递方式、渠道状态）入库，与用户个人素材隔离单独目录。用途：投递决策参考（哪类岗位多、回复率）、生成简历的 JD 参考。

**岗位确认（冷启动首轮做一次）**：把择优后的 N 个候选（公司/岗位/方向/投递方式/渠道）给用户看一次确认方向。确认后沉淀为偏好，后续轮次按此推进不再列（用户主动要看随时给）。

## 阶段 2 素材整理（agent 自主 + 用户协作）

**目标**：把用户的真实产出整理成可复用的「简历模块」（素材卡）。

**纪律（核心：真实性 + 可查证）**：

- **每条素材必须可查证**——用户说"运营过账号"→ 要具体数据（粉丝量/播放量/转化率）；说"做过项目"→ 要项目链接/产出/角色。模糊的追问到具体为止。
- **能力维度 ≥2 素材支撑**——一个能力主张至少 2 个独立素材佐证，单一孤证降级处理（标注"疑似"或追问补充）。
- **素材卡格式**：[能力维度] → 素材描述（产出+量化+来源）。例：`[数据分析] → 分析 10w+ 用户行为数据（SQL），发现 X 规律，转化率提升 15%（来源：某项目报告）`。
- **绝不虚构素材**——用户没有的绝不脑补。缺素材就降级表述或追问用户，不在简历里编。
- 组织素材时可参照 `templates/sample-resume/` 的写法范式（按需读取）。

## 阶段 3 组合简历（模板 → 模块 → 适配岗位）

**模板策略（必读，先问用户）**：
- 用「默认简历模板」还是「自定义偏好」？**先问，不要直接默认**
- **默认模板**：随 skill 分发的 `templates/resume-modern/resume.html`（A4 单页、苹果风、占位符版）——只填占位符，不改变结构；模板结构规范见同目录 `SKILL.md`
- **自定义偏好**：用户选视觉风格（`templates/resume-modern/styles/` 有 6 种：苹果/极简/小红书/复古/商务/创意）→ 套用对应 tokens.css 调整颜色字体，布局/栏目也可按用户偏好改，见 `templates/resume-modern/README.md`
- 模板版权与来源：`templates/resume-modern/README.md` 文末（Open Design Apache-2.0）

**流程**：
1. 确认模板（默认 or 自定义）→ 用模板建「底板简历」（1-2 份高质量底版，作为质量基准）
2. 每岗位：从模块库选相关模块 → 与底板混合 refine（补充/替换/定制段落）→ 成品简历
3. 导出 PDF（或岗位要求的格式）

**纪律**：
- **从模块拼装 ≠ refine**：底版是质量基准，模块只做补充/定制，不从零拼装
- 金融/专业素材按岗位定制（音乐/内容岗不写金融，金融/产品岗才加）
- 内容永远按用户真实素材组织（不虚构、不拼装）
- 写法参照 sample 简历范式：岗位→范围→量化产出→持续迭代

**PDF 生成（用规定模板，不让 agent 自由生成）**：
- 直接用随包模板 `templates/resume-modern/resume.html`（A4 单页、苹果风）——agent 只编辑 HTML 内容（占位符/模块），不改样式结构
- 原因：让 agent 从零生成 PDF 很容易出代码/前端问题，即便代码对美观也不足——模板是我们整合的资源，直接用最好
- 转换：HTML → PDF，目标单页 A4（浏览器打印或 HTML→PDF 工具，agent 自选）

## 阶段 4 投递（邮箱直投为主，方式按环境发现）

**目标**：把简历 + 定制邮件发到每个岗位的投递邮箱。

**邮件生成（核心规则，必须遵守）**：
```
Subject: 应聘{岗位} - {姓名} - {可用性一句话}
正文:
【申请方向】我申请【{方向}】，因为：
  1. {匹配理由1，具体素材}
  2. {匹配理由2，具体素材}
  3. {匹配理由3，具体素材}
【一句话介绍】{学校}（{毕业时间}毕业）
【安排】{可用性}，base {城市}（可远程）
【作品集】{如有}
简历详见附件...
```

**方向判定**：从岗位标题/描述关键词自动判断（产品/增长/运营/数据/AI...），匹配理由从模块库抽。

**发送方式发现（思路：先本机 → 找开源 → 浏览器兑底）**：
```
1. 先探测用户环境有什么（本机邮件客户端 / SMTP / 网页邮箱 / 邮件 API）
2. 选一个能用且最简单的，向用户说明【为什么这么做】（让用户有知情权：用最简单可靠的方式投递，不折腾环境）
3. 没有 → 搜 GitHub 同类工具 → 再不行 → 浏览器操作网页邮箱兑底
```

**投递纪律**：
- **单封测试先行**（首封验证到达再批量）
- 批量间隔几秒，防限流
- 发件账户 + 签名统一

## 阶段 5 记录 + 跟进 + 沉淀（双轨沉淀）

**沉淀有两方面，都要做**：

1. **市场/投递信息沉淀**：每封投递记录（时间/收件人/岗位/附件版本/方向）到 `{工作目录}/applications.jsonl`；岗位信息沉淀到素材库（与用户个人素材隔离）；邮件文案按轮次归档。用途：用户随时知道投递情况、决策参考（回复率）、优化文案、准备面试。
2. **执行沉淀（agent 错题集）**：退信/回复/执行问题 → 写入 `log.md`（带日期+问题+修复+适用阶段）；**agent 下轮先读错题本**，用它 refine 自己的操作（避免重犯）——这是 agent 自我迭代的机制。

**跟进**：定期检查收件箱，收到回复 → 整理对方背景 → 起草回复。**收件箱怎么查**：复用阶段4 的发送通道反向检查——本机邮件客户端收信 / 网页邮箱收件箱 / 邮件 API 拉取，哪个通就用哪个（发送方式发现流程同理）。

**边界（重要，避免误伤自动化）**：此处「用户确认后才发出」**仅针对跟进回复且涉及承诺**（薪资/入职时间/提供材料/可到岗等有法律或信任后果的承诺），且**只约束「回复」环节——阶段4投递的主动批量投递不在此列，照常自动执行**。涉及承诺的回复绝不擅自发出（替用户承诺有后果）；不涉及承诺的普通跟进可由 agent 草拟并简要告知用户即可，不必逐封等待确认。

## Self-Refine（自修补机制，贯穿全程）

**原则**：遇到问题不卡死、不跳过——诊断 → 修复 → 继续。修复的经验建议记入经验库（见 log.md），成为下轮已知坑。

### 自检点（每步完成后）

| 阶段 | 自检 | 异常处理 |
|---|---|---|
| 初始化 | **硬放行检查：采集工具配齐 ≥3 个了吗？**（求职CLI 装好+实测跑通？浏览器自动化实测能截图？web_search 能用？）**我有真实视觉能力吗（不是在猜）？** | 配不齐→不许进阶段1，回去装（未实测就说不通＝偷懒）；视觉能力存疑→诚实声明并配真视觉模型或改纯文本提取 |
| 复用检查 | **进入本阶段前查 log.md 索引并报出适用经验卡了吗？**当前情境触发哪几张？ | 没查→回去查索引+对照触发信号；报出 EXP-XXX 和是否触发 |
| 找岗位·采集 | **采集池总数 ≥ N×2 了吗？**（不到不许进择优）**是不是只用了一个渠道凑够就停？是不是把"评估"当成了"尝试"？**（只评估未实测＝未完成） | 采集池不够 → 回去补打其他渠道直到 ≥N×2；单渠道凑够 → 补打其他渠道 |
| 找岗位·社交平台 | 是否**进了详情页**提取投递方式？列表页截图直接入库是错的 | 补详情页二次提取 |
| 找岗位·视觉 | 我声称的视觉结果**是真的转写还是我在编**？读不了图绝不编造 | 编造→停止，诚实声明并换路线 |
| 找岗位·验证 | 高风险信号筛查了吗？可疑岗做了工商核验吗？ | 高风险剔除；可疑补工商核验 |
| 找岗位·择优 | 是从候选池择优 N 个，还是边采边投够 N 就停？ | 边采边投 → 改为先充分采集再择优 |
| 素材整理 | 每素材可查证？能力 ≥2 素材？ | 不足 → 降级表述/问用户 |
| 组合简历 | 模块与岗位匹配？质量达标？ | 换模块/回底版 refine |
| 投递 | 首封测试过？有退信？ | 修邮箱/换发送方案 |
| 记录 | 日志一致？ | 合并去重 |

### 工具替代发现（核心机制）

**当某阶段没有可用工具时**：走阶段 0b 的「工具不够用 → 以样本为参考」发现机制（查工具样本库 → web_search 搜同类高 star 项目 → 小规模验证），额外补充：评估（能达成该阶段目标? 配置成本? 生态?）+ 记录（适配方案写回 env.json + log.md 供下轮复用）。

**判断标准**：只要求达成阶段目标，不追求与原实现一致。**不写死**：任何工具都只是当前环境的临时选择，换环境重新探测。

### 修补记录规范

每修补写 log.md。**不打断用户**，除非：需要新权限/凭据（如 SMTP 授权码）；涉及金钱/承诺/个人信息对外发布；连续 3 次同类修补失败（需重新设计）。

## 经验库机制（复用为核心，详见 log.md）

经验库文件 `log.md`（结构化经验卡 + 情境索引 + 收敛规则）是 agent 自我迭代的核心。**复用机制、记录规范、收敛规则（去重/合并/升级/老化）的完整细节见 `log.md`**——这里只留执行要点：

**log.md 路径设置（使用前确认）**：log.md 随 skill 包分发，**默认路径 = 与 SKILL.md 同目录**（skill 包内）。agent 使用时：先确认本环境 log.md 在 skill 目录下（找不到 → 在工作目录找 → 都没有 → 把 skill 目录的 log.md 复制到工作目录并以此为路径）。**同一轮内所有阶段读写同一份 log.md**，不要各阶段各写各的副本。

- **复用流程**：进入每个阶段前，按阶段索引检索 log.md 顶部「情境索引」表，定位本阶段适用的经验卡 → 对照触发信号判断是否命中 → 命中则照对策执行。
- **复用自检（每阶段开始）**：用一句话报出——「本阶段适用经验卡：EXP-XXX；当前情境触发：哪几张；照对策做了什么」。没命中报「无适用经验」。
- **记录**：每轮结束把「新问题 + 修复」写成结构化经验卡追加到 log.md（触发信号要具体，否则无法被复用检索）。
- **升级**：同一卡重复触发 ≥3 次（说明靠经验提醒不够，必须变成强制规则），或对策已稳定可靠 → 把对策写进正文规则，**然后从 log.md 删除该卡**（正文已是唯一来源，不重复存）。

## 已知坑

完整经验卡见 `log.md`（EXP-001~003 + 情境索引 + 收敛规则），执行时按经验库机制复用。各项坑的对策已融入正文相应阶段（采集解耦/视觉诚实/详情页/风险分级等），Windows 跨平台提示见 GUIDE.md（启动障碍）。
