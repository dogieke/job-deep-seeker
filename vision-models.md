# 视觉转写模型选型参考

> 反爬截图转写需要的多模态视觉模型选型参考。**不是写死的推荐**——价格约 3 个月一变、可用性随环境变，运行时仍按当时环境/价格复核。本文件只提供"可选清单 + 实测证据 + 选型方法"，具体用哪个由用户拍板（见 SKILL.md 阶段1）。

## 选型方法（可靠且不繁重）

- **禁止**：不写批量对比脚本、不跑多模型算准确率、不做性价比表（过度工程）。
- **怎么做**：候选（用户给的或 subagent 推荐的）**每个用一张真实中文小样本反爬图跑一次**，只看**关键字段（中文岗位名 + 邮箱）是否转写正确**——正确即可用；乱码/失败/返回空即淘汰换下一个。**都转写正确时，选价格最低的**。
- **价格从哪来**：优先读环境已配模型的价格（如 models.json 的 cost 字段：单价或订阅内 0），不确定真实市场价时由 subagent 查证（web_search 官方定价页）。
- **可靠性依据**：好模型与坏模型的差异是悬崖式——一次小样本验证足以捕获不可用模型，不需要统计对比。

## 实测证据（2026-08，真实含噪声中文反爬图）

测试方式：3 张含噪测试图 × 3 次调用 × 90 字段，逐字段自动比对 ground truth。

| 模型 | 模型准确率（成功时） | 调用成功率 | 备注 |
|---|---|---|---|
| gemini-2.5-flash-lite | 100% (80/80) | 89% (8/9) | 便宜；OpenRouter 通道偶发不稳定 |
| glm-4.5v | 100% (90/90) | 100% (9/9) | 便宜；调用稳定 |
| gpt-4o-mini | 99% (89/90) | 100% (9/9) | 便宜；调用稳定 |
| glm-4.6v | 返回空 | — | 不可靠，排除 |

**结论**：低成本多模态就够用，**不追贵的**——选一个环境里现成、便宜、实测能稳定转写中文的即可。

## 专用 OCR 模型（理论更便宜更专，需实测确认）

专门 OCR 多模态模型理论上比通用多模态便宜一个量级且中文更专，但需按当前环境实测确认可用性：

- **GLM-OCR**（智谱）：¥0.2/百万，理论最便宜
- **Qwen-VL-OCR**（阿里云）：¥0.3/¥0.5，国内+海外五区域同开、全球可用性最广
- 免费兜底：GLM-4.1V-Thinking-Flash 或自部署开源 GLM-OCR

（注：此处「OCR」指多模态 OCR 视觉模型，非 tesseract 传统 OCR，两者不同）
